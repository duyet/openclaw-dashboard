name = "openclaw-mission-control"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"

[vars]
# NEXT_PUBLIC_AUTH_MODE is a build-time variable (inlined by webpack).
# Set it in .env.local for local dev, or as a GitHub Actions secret for CI.
# Setting it here has no effect.
LOCAL_AUTH_TOKEN = "change-me-to-a-secure-token-at-least-50-chars-long-please"

[[d1_databases]]
binding = "DB"
database_name = "openclaw-mission-control"
database_id = "656eb2a0-00e3-4dac-a384-8b086ae80151"
migrations_dir = "drizzle/migrations"

[[kv_namespaces]]
binding = "KV"
id = "dd84f5cc51dc4ffe90132e03fff9ad20"
preview_id = "43eebe6b333a41a581b0818a0f7ce234"

# Queues: create the queue first with:
#   npx wrangler queues create webhook-delivery
# then uncomment:
# [[queues.producers]]
# binding = "WEBHOOK_QUEUE"
# queue = "webhook-delivery"
#
# Queue consumer must be a separate Worker — see workers/queue-consumer.ts
# [[queues.consumers]]
# queue = "webhook-delivery"
# max_batch_size = 10
# max_retries = 3
# dead_letter_queue = "webhook-delivery-dlq"
